echo "$STR" | grep -o "[^ ]\+\( \+[^ ]\+\)*"

"\( \+[($]\)"


grep -E -r "(gl|egl).+.\(" gltxt.txt -o|sed 's/($//'|sort -n|uniq -c
echo "   abc   def   " | sed 's/^ *\| *$//g'

awk -F '(' '{print $1}' gltxt.txt

